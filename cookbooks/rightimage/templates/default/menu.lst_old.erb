default         0
timeout         0
hiddenmenu

<% kern = ::File.basename(Dir.glob("/mnt/#{node[:rightimage][:cloud]}/boot/vmlinuz*").sort.last) %>
<% initrd = ::File.basename(Dir.glob("/mnt/#{node[:rightimage][:cloud]}/boot/initrd*").sort.last) %>
title           <%= node[:rightimage][:platform] %> (<%= kern %>)
root            (hd0)
kernel          /boot/<%= kern %> root=/dev/sda1 ro console=hvc0
initrd          /boot/<%= initrd %>
