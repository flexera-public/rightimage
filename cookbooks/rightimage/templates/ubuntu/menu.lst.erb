default         0
timeout         0
hiddenmenu

<% kern = ::File.basename(Dir.glob("/mnt/image/boot/vmlinuz*").first) %>
<% initrd = ::File.basename(Dir.glob("/mnt/image/boot/initrd*").first) %>
title           Ubuntu
root            (hd0)
kernel          /boot/<%= kern %> root=/dev/sda1 ro console=hvc0
initrd          /boot/<%= initrd %>
